# Requirement traceability

Generated by `./scripts/check-trace.py --write`; `mise run trace:check` fails when
this file is stale or a traced requirement is missing a layer. Requirements are
defined in `spec/core.symspec.json`.

| Requirement | model | gherkin | fuzz | test | impl |
|---|---|---|---|---|---|
| CLI-7 | 1 | 1 | 1 | 4 | 5 |
| CLI-8 | 1 | 1 | 1 | 2 | 3 |
| CLI-9 | 1 | 1 | 1 | 2 | 3 |

## CLI-7

If a reader closes the CLI's stdout or stderr, then the CLI crate shall exit with a status from its exit table and neither panic nor terminate by signal.

- **model:** `model/src/output.rs`
- **gherkin:** `microvms-cli/tests/features/closed_output.feature`
- **fuzz:** `microvms-cli/src/closed_output_fuzz.rs`
- **test:** `microvms-cli/src/closed_output.rs`, `microvms-cli/src/guards.rs`, `microvms-cli/tests/bdd.rs`, `microvms-cli/tests/thinness.rs`
- **impl:** `microvms-cli/src/closed_output.rs`, `microvms-cli/src/envelope.rs`, `microvms-cli/src/main.rs`, `microvms-core/src/lib.rs`, `microvms-core/src/sandbox.rs`

## CLI-8

If the reader of stdout or stderr closes after a command has started, then the CLI crate shall perform every teardown and ledger write that command owes and exit with the code of the command's own outcome, except for a streaming command whose stdout reader closes.

- **model:** `model/src/output.rs`
- **gherkin:** `microvms-cli/tests/features/closed_output.feature`
- **fuzz:** `microvms-cli/src/closed_output_fuzz.rs`
- **test:** `microvms-cli/src/guards.rs`, `microvms-cli/tests/bdd.rs`
- **impl:** `microvms-cli/src/closed_output.rs`, `microvms-cli/src/envelope.rs`, `microvms-cli/src/main.rs`

## CLI-9

If the stdout reader of a streaming command closes, then the CLI crate shall stop streaming within one further event, leave the remote exec running, and exit with ERR_INTERRUPTED naming the exec id on stderr.

- **model:** `model/src/output.rs`
- **gherkin:** `microvms-cli/tests/features/closed_output.feature`
- **fuzz:** `microvms-cli/src/closed_output_fuzz.rs`
- **test:** `microvms-cli/src/guards.rs`, `microvms-cli/tests/bdd.rs`
- **impl:** `microvms-cli/src/closed_output.rs`, `microvms-cli/src/commands/attached.rs`, `microvms-cli/src/envelope.rs`
